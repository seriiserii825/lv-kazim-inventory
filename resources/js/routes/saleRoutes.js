import SaleIndex from "./../pages/sales/Index.vue";

const salesRoutes = [
    {
        path: "/admin/sales",
        component: SaleIndex,
        name: "admin.sales",
    },
];

export default salesRoutes;
